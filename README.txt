LAMBA IMAGE STUDIO — REMOTE IMAGE EDITOR 2.0

Render:
Build Command: npm install
Start Command: npm start
Environment Variable:
REPLICATE_API_TOKEN = your Replicate token

Do NOT put the token in index.html.

The app accepts a photo + text instruction at POST /api/generate and sends it to FLUX.1 Kontext Pro.
